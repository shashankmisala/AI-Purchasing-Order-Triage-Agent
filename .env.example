# Copy this file to .env and fill in your key to enable LLM-assisted classification,
# action recommendation, and email drafting. If left unset, the agent runs entirely
# on its rule-based engine (see PRD section 3.2 hybrid design) -- no key required.
ANTHROPIC_API_KEY=
